# Knowledge Drop — Frontend env
# VITE_API_BASE is read at build and dev time by Vite; the app falls back to http://127.0.0.1:8000 if unset.
# For cookie-based auth, make sure this matches the backend host you actually use in the browser (prefer localhost).
VITE_API_BASE=http://localhost:8000